from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a small project for prediction the emotion of the sentence',
    author='prasann_vardhan',
    license='',
)
